{
    "version": "0.2.0",
    "configurations": [
      {
        "type": "dotnetwatchattach",
        "request": "launch",
        "name": ".NET Watch Attach",
        "args": { // Args to pass to coreclr attach
          "env": {
            "ASPNETCORE_ENVIRONMENT": "Development"
          }
        },
        "task": "genealogyWatch", // Label of watch task in tasks.json
        "program": "genealogy-ssr" // for windows Or "<startup-project-name>" for linux
      }
    ]
  }